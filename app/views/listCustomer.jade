extends layout

block content
    
	div
		- for(var i = 0; i < customers.length; ++i){

			div.item
				h2 #{customers[i].ficha}
				p.idCustomer #{customers[i].cedula} 
				div.myButton.f1
					a(href="takeFile?id=#{customers[i]._id}") Tomar Ficha
		-}